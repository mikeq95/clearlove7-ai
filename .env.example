# Clerk publishable key, used for the sign-in UI in the sidebar/settings.
# Get one at https://dashboard.clerk.com (Clerk project → API Keys → Publishable key).
# Sign-in is optional for end users — /api/explain only needs the AI provider
# API key each user enters in Settings, not a Clerk session.
VITE_CLERK_PUBLISHABLE_KEY=
